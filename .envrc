# Activate mise (Python, uv) and project .venv when entering this directory.
# Requires: direnv (eval "$(direnv hook zsh)" in ~/.zshrc). Run 'direnv allow' once.
eval "$(mise activate bash)"
source .venv/bin/activate
